name: Commit Guard

# 只允许手动触发，推送/PR 不再自动触发
on:
  workflow_dispatch:

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - name: Skip guard (temporary)
        run: echo "Commit Guard is temporarily disabled"
